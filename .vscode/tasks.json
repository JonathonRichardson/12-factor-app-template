{
    "version": "2.0.0",

    "tasks": [
        {
            "label": "new react folder and files",
            "command": "mkdir -p ${workspaceFolder}/${input:dirName} && touch '${workspaceFolder}/${input:dirName}/${input:dirName}.component.jsx' '${workspaceFolder}/${input:dirName}/${input:dirName}.styles.jsx'",
            "type": "shell",
            "problemMatcher": [],
            "presentation": {
                "echo": false,
                "reveal": "silent",
                "focus": false,
                "panel": "shared",
                "showReuseMessage": false,
                "clear": true
            }
        }
        // {
        //     "type": "npm",
        //     "script": "lint:client",
        //     "problemMatcher": [],
        //     "label": "npm: lint:client"
        // }
    ],

    // ........................................................................................

    "inputs": [
        {
            "type": "promptString",
            "id": "dirName",
            "description": "Complete my folder name",
            "default": "jsx folder to create"
        }
    ]
}
